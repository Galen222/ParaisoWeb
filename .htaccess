# Deshabilitar la visualización de directorios
Options -Indexes

# Bloquear acceso a archivos sensibles
<FilesMatch "^(?:\.htaccess|\.htpasswd|\.env(?:\..*)?|.*\.(?:log|ini))$">
Require all denied
</FilesMatch>

# Ocultar la versión de Apache y Next.js
ServerSignature Off
